apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: redis.k8s.amaiz.com
status: {}
spec:
  group: k8s.amaiz.com
  names:
    plural: redis
    singular: redis
    kind: Redis
    listKind: RedisList
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          required:
            - spec
          properties:
            apiVersion:
              description: >-
                APIVersion defines the versioned schema of this representation
                of an object. Servers should convert recognized schemas to the
                latest internal value, and may reject unrecognized values. More
                info:
                https://git.k8s.io/community/contributors/devel/api-conventions.md#resources
              type: string
            kind:
              description: >-
                Kind is a string value representing the REST resource this
                object represents. Servers may infer this from the endpoint the
                client submits requests to. Cannot be updated. In CamelCase.
                More info:
                https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds
              type: string
            metadata:
              description: >-
                Standard object's metadata. More info:
                https://git.k8s.io/community/contributors/devel/api-conventions.md#metadata
              type: object
            spec:
              type: object
              required:
                - replicas
                - redis
              properties:
                affinity:
                  description: Pod affinity
                  type: object
                annotations:
                  description: Pod annotations
                  type: object
                  additionalProperties:
                    type: string
                config:
                  description: Config allows to pass custom Redis configuration parameters
                  type: object
                  additionalProperties:
                    type: string
                dataVolumeClaimTemplate:
                  description: DataVolumeClaimTemplate for StatefulSet
                  type: object
                exporter:
                  description: Exporter container specification
                  type: object
                  required:
                    - image
                  properties:
                    image:
                      description: Image is a standard path for a Container image
                      type: string
                    initialDelaySeconds:
                      description: >-
                        Number of seconds after the container has started before
                        liveness probes are initiated. More info:
                        https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes
                      type: integer
                      format: int32
                    resources:
                      description: Resources describes the compute resource requirements
                      type: object
                    securityContext:
                      description: >-
                        SecurityContext holds security configuration that will
                        be applied to a container
                      type: object
                imagePullSecrets:
                  description: >-
                    Pod ImagePullSecrets More info:
                    https://kubernetes.io/docs/concepts/containers/images#specifying-imagepullsecrets-on-a-pod
                  type: array
                  items:
                    type: object
                initContainers:
                  description: Pod initContainers
                  type: array
                  items:
                    type: object
                nodeSelector:
                  description: >-
                    NodeSelector specifies a map of key-value pairs. For the pod
                    to be eligible to run on a node, the node must have each of
                    the indicated key-value pairs as labels.
                  type: object
                  additionalProperties:
                    type: string
                password:
                  type: object
                  required:
                    - secretKeyRef
                  properties:
                    secretKeyRef:
                      description: >-
                        SecretKeyRef is a reference to the Secret in the same
                        namespace containing the password.
                      type: object
                priorityClassName:
                  description: Pod priorityClassName
                  type: string
                redis:
                  description: Redis container specification
                  type: object
                  required:
                    - image
                  properties:
                    image:
                      description: Image is a standard path for a Container image
                      type: string
                    initialDelaySeconds:
                      description: >-
                        Number of seconds after the container has started before
                        liveness probes are initiated. More info:
                        https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes
                      type: integer
                      format: int32
                    resources:
                      description: Resources describes the compute resource requirements
                      type: object
                    securityContext:
                      description: >-
                        SecurityContext holds security configuration that will
                        be applied to a container
                      type: object
                replicas:
                  description: Replicas is a number of replicas in a Redis failover cluster
                  type: integer
                  format: int32
                  minimum: 3
                securityContext:
                  description: Pod securityContext
                  type: object
                serviceAccountName:
                  description: >-
                    Pod ServiceAccountName is the name of the ServiceAccount to
                    use to run this pod. More info:
                    https://kubernetes.io/docs/tasks/configure-pod-container/configure-service-account/
                  type: string
                tolerations:
                  description: Pod tolerations
                  type: array
                  items:
                    type: object
                volumes:
                  description: Volumes for StatefulSet
                  type: array
                  items:
                    type: object
            status:
              type: object
              required:
                - replicas
                - master
              properties:
                master:
                  description: Master is the current master's Pod name
                  type: string
                replicas:
                  description: >-
                    Replicas is the number of active Redis instances in the
                    replication
                  type: integer
                  format: int64
      subresources:
        status: {}
        scale:
          specReplicasPath: .spec.replicas
          statusReplicasPath: .status.replicas
      additionalPrinterColumns:
        - name: Master
          type: string
          description: Current master's Pod name
          jsonPath: .status.master
        - name: Replicas
          type: integer
          description: Current number of Redis instances
          jsonPath: .status.replicas
        - name: Desired
          type: integer
          description: Desired number of Redis instances
          jsonPath: .spec.replicas
        - name: Age
          type: date
          jsonPath: .metadata.creationTimestamp

